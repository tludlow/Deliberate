import { expect } from 'chai'

describe('User Authentication', () => {
    it('should create a new user', () => {
        expect(1).to.equal(1)

        expect({ hi: 'saranyan' }).to.equal({ jesus: 'christ' })
    })
})
