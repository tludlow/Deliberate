version: '3'

services:
    database:
        image: 'postgres'
        environment:
            POSTGRES_USER: deliberate
            POSTGRES_PASSWORD: randompasswordlol
        volumes:
            - db-data:/var/lib/postgresql/data
        ports:
            - '5432:5432'

volumes:
    db-data:
