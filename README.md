# Deliberate

An automated small-team management solution for agile software engineering teams

### Backend proxy

The backend is connected to through a proxy, use localtunnel to do this and run the command: `npx localtunnel --port 5000 --subdomain deliberate-proxy`
